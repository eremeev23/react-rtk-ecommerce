import React from 'react';
import styled from "styled-components";

const Item = styled.li`

`

const PopularOneItem = ({item}) => {
  return (
    <Item>

    </Item>
  );
};

export default PopularOneItem;